import React from 'react';
import cl from "./BigPoster.module.scss"

const BigPoster = () => {
    return (
        <div className={cl.poster}>
            
        </div>
    );
};

export default BigPoster;