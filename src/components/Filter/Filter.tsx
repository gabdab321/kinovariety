import React from 'react';
import Slider from "../UI/Slider/Slider";

const Filter = () => {
    return (
        <div>
            select
            <Slider min={0} max={10}/>
        </div>
    );
};

export default Filter;